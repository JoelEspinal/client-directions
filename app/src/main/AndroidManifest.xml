<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".app.ContactApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ClientInfo">
        <activity
            android:name=".activities.SplashActivity"
            android:theme="@style/SplashTheme">
<!--            <intent-filter>-->
<!--                <action android:name="android.intent.action.MAIN" />-->

<!--                <category android:name="android.intent.category.LAUNCHER" />-->
<!--            </intent-filter>-->
        </activity>
        <activity android:name=".activities.AddressActivity"
            android:label="@string/address_form"/>
        <activity android:name=".activities.ClientFormActivity"
            android:label="@string/info_form"/>
        <activity android:name=".activities.ClientActivity"
            android:label="@string/clients"/>
        <activity android:name=".activities.ContactActivity"
            android:label="@string/contacts">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.ContactFormActivity"
            android:label="@string/contacts"/>
    </application>

</manifest>